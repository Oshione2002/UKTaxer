{
  "name": "uktaxer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "NTaxer-based UK tax calculators and law library",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/validate.mjs && node scripts/build.mjs",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "read-excel-file": "^5.8.6"
  }
}
