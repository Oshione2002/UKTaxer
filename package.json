{
  "name": "uktaxer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && node scripts/write-precache.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build",
    "law:sync": "node scripts/sync-law.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@fontsource/dm-sans": "^5.3.0",
    "@fontsource/manrope": "^5.3.0",
    "jszip": "^3.10.2",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@xmldom/xmldom": "^0.9.9",
    "vite": "^8.3.1"
  }
}
